import ContactView from "@/components/ContactView";

export const metadata = {
  title: "Contact — CleanNest",
  description: "Get in touch with CleanNest for bulk orders and questions.",
};

export default function ContactPage() {
  return <ContactView />;
}
