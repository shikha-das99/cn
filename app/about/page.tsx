import AboutView from "@/components/AboutView";

export const metadata = {
  title: "About — CleanNest",
  description:
    "CleanNest formulates and ships housekeeping essentials direct to your door — concentrated, honestly priced, no distributor markup.",
};

export default function AboutPage() {
  return <AboutView />;
}
